import { DetailssearchPipe } from './detailssearch.pipe';

describe('DetailssearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailssearchPipe();
    expect(pipe).toBeTruthy();
  });
});
